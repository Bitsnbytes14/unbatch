# Copy this file to .env and drop your own key in — adjudicator.py loads
# .env automatically via python-dotenv, so no shell configuration is needed.
# .env itself is gitignored; never commit a real key.
OPENAI_API_KEY=
